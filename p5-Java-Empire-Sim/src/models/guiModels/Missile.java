package models.guiModels;

import javafx.scene.image.Image;

public class Missile extends GameObject{
    public Missile(Image image, double width, double height, double x, double y) {
        super(image, width, height, x, y);
    }
}
